tup = (1, 4, 5, 6)

res = len(set(tup)) == len(tup)

print('Is tuple distinct?:', res)

'''
 Python3 code to test if tuple is distinct using set() + len()

tup = (1, 4, 5, 6)

# printing original tuple
# print("The original tuple is : " + str(tup))

# test if tuple is distinct using set() + len()

res = len(set(tup)) == len(tup)

print('Is tuple distinct?:', res)
'''
